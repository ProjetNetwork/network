{% extends 'base.html.twig' %}

{% block event %}
    <h1>Evenements</h1>
        <a href="{{ path('evenement_new') }}">Créer un nouvel évenement</a>
        {% for evenement in evenements %}<nav class="navbar navbar-default evenement-container-collapse" style="background-color: #000000>
      <button class="btn" data-toggle="collapse" data-target="#demo{{evenement.name}}"><i><span id="collapse-span" class="glyphicon glyphicon-menu-down"  style="font-size: 35px;"></span></i>
      <p class="evenement-text-collapse">{{evenement.name}}</p></button>
      <div id="demo{{evenement.name}}" class="collapse">
            <div class="well">
                <div class="well" style="background-color: white;">
                    <h3>{{ evenement.message }}</h3>
                </div>
                <a href="{{ path('evenement_show', { 'id': evenement.id }) }}">Voir</a></br>
                <a href="{{ path('evenement_edit', { 'id': evenement.id }) }}">Editer</a>
            </div>
                  </div>
    </nav>
        {% endfor %}
{% endblock %}
{% block leftmenu %}
  <div class="row content">
      <ul class="nav nav-pills nav-stacked">
        <a class="btn  btn--icon  btn--add" href="{{ path('evenement_index')}}"><i><span class="glyphicon glyphicon-th-list"></span></i><span>Liste</span></a>
        <a class="btn  btn--icon  btn--add" href="{{ path('evenement_new')}}"><i><span class="glyphicon glyphicon-plus-sign"></span></i><span>Creer un event</span></a>
        <a class="btn  btn--icon  btn--add" href="{{ path('evenement_new')}}"><i><span class="glyphicon glyphicon-plus-sign"></span></i><span>Event du jour</span></a>
      </ul>
  </div>
{% endblock %}